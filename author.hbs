{{!< default}}
{{!-- The tag above means: insert everything in this file
into the {body} of the default.hbs template --}}

{{#author}}
    {{> "header" author-count=../pagination.total}}
{{/author}}
{{!-- Main --}}
<main  class="sm-grid sm-author-posts-container" id="sm-main">
    <div class="sm-basic-grid">
        {{#foreach posts}}
            {{> "card"}}
        {{/foreach}}
    </div>
</main>

{{pagination}}