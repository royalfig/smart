<article class="sm-card {{#is "home"}}sm-card-{{@number}}{{/is}} {{slug}} {{#if horizontal}}sm-card-horizontal{{else}}sm-card-vertical{{/if}}">
    <a class="sm-card-link" href="{{url}}" aria-label="{{title}}">
        {{#if horizontal}}
            {{#if feature_image}}<img srcset="{{img_url feature_image size="xs"}} 300w, 
                {{img_url feature_image size="s"}} 500w,
                {{img_url feature_image size="m"}} 800w,
                {{img_url feature_image size="l"}} 1000w"
                sizes="(min-width: 750px) 500px, 100vw"
                src="{{img_url feature_image size="s"}}"
                alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}">{{/if}}
            <div class="sm-card-body">
                <div>
                    <h2 class="sm-title {{^if feature_image}}large{{/if}} {{#if premiere}}large{{/if}}">{{title}}</h2>
                    {{#if custom_excerpt}}
                        {{^if premiere}}
                            {{^if feature_image}}
                            <p class="sm-excerpt">{{custom_excerpt}}</p>
                            {{/if}}
                        {{/if}}
                        {{#if premiere}}<p class="sm-excerpt">{{custom_excerpt}}</p>
                        {{/if}}
                    {{/if}}
                </div>
                <div class="sm-card-footer">
                    {{#if primary_tag}}<p class="sm-tag">{{primary_tag.name}}</p>{{else}}
                    <div>
                        <p class="sm-reading-time">{{reading_time}}</p>
                    </div>{{/if}}
                </div>
            </div>
        {{else}}
            <div>
                {{#if feature_image}}<img srcset="{{img_url feature_image size="xs"}} 300w, 
                {{img_url feature_image size="s"}} 500w,
                {{img_url feature_image size="m"}} 800w,
                {{img_url feature_image size="l"}} 1000w"
                sizes="(min-width: 750px) 500px, 100vw"
                src="{{img_url feature_image size="s"}}"
                alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}">{{/if}}
                <div class="sm-card-body">
                    <h2 class="sm-title {{^if feature_image}}large{{/if}}">{{title}}</h2>
                    {{^if feature_image}}<p class="sm-excerpt">{{excerpt}}</p>{{/if}}
                </div>
            </div>
            
            <div class="sm-card-footer">
                {{#if primary_tag}}<p class="sm-tag {{primary_tag.slug}}">{{primary_tag.name}}</p>{{else}}
                <p class="sm-reading-time">{{reading_time}}</p>{{/if}}
            </div>
        {{/if}}
    </a>
    <button class="sm-circle-icon-button sm-love-button" data-id="{{slug}}" data-title="{{title}}" aria-label="Save {{title}} to favorites">
        <span class="sm-heart-outline-icon">
            <svg><use href="#sm-heart-outline-icon"></use></svg>
        </span>
        <span class="sm-heart-fill-icon">
            <svg><use href="#sm-heart-fill-icon"></use></svg>
        </span>
    </button>
</article>